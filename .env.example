# Example Environment Variables
# Copy this to .env and fill with your actual values

# Cloudflare Secret for encryption (used in wrangler.toml or set via dashboard)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
CF_SECRET=your-secret-key-here

# Note: For production, set secrets via:
# wrangler secret put CF_SECRET
